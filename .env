# Database Configuration
POSTGRES_DB=pacsdb
POSTGRES_USER=pacs
POSTGRES_PASSWORD=pacs

# Archive Configuration
ARCHIVE_DEVICE_NAME=dcm4chee-arc
ARCHIVE_AET=DCM4CHEE

# Java Memory Settings (adjust based on your available RAM)
JAVA_OPTS=-Xms256m -Xmx1024m -XX:MetaspaceSize=96M -XX:MaxMetaspaceSize=256m -Djava.net.preferIPv4Stack=true

# Timezone (optional - uncomment and adjust if needed)
# TZ=America/New_York
